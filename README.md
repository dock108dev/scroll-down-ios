# Scroll Down Sports — iOS App

Spoiler-free sports recaps for iOS, built with SwiftUI and MVVM.

## Local Development

**Requirements:** Xcode 15+, iOS 16+

```bash
# Open in Xcode
open ScrollDown.xcodeproj

# Or build from command line
xcodebuild -scheme ScrollDown -destination 'platform=iOS Simulator,name=iPhone 16' build
```

## Deployment Basics

Archive the app in Xcode and distribute via TestFlight or the App Store.

## Documentation

Deeper documentation lives in [`/docs`](docs/README.md). Core project references like
`AGENTS.md` remain at the repo root, and the changelog lives in [`/docs/CHANGELOG.md`](docs/CHANGELOG.md).
