// Copy to Config/Local.xcconfig for local simulator or direct-device builds.
// Config/Local.xcconfig is ignored by git and may contain private credentials.
SDA_API_BASE_URL = https:/$()/sda.dock108.dev
SDA_API_KEY =
SDS_DEVELOPMENT_TEAM =
